import { createMuiTheme } from '@material-ui/core/';

export const theme = createMuiTheme({
    palette:{
        primary: {main: '#fff'},
        secondary: {main : '#f25759'},
        text:{
            primary: '#222',
            secondary: '#888',
        },
        background:{
            default: '#fff',
        }
    }
});